# 2026-09-23 — Custom user field sync (hub)

- Added `customer_user_fields`, `customer_user_field_values`, and `user_field_sync_logs`.
- Canonical `/api/v1/sync/user-fields` (+ archive/restore) and `/api/v1/sync/user-field-values` with LMS hub list endpoints.
- Outbound pusher targets CMS + Firearm subscription types and optional LMS hub (`config/user_field_sync.php`).
- Documented contract in `docs/TENANT_SYNC_INTEGRATION.md` §5b.
