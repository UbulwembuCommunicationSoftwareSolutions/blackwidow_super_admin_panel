# Super Admin — LMS hub env template

- Added `Type12LmsRequiredEnvSeeder` (subscription type 12) with `SECURE_TOKEN`, `SUPERADMIN_API_URL`, and LMS app defaults.
- Hub reconcile endpoints: `GET /api/v1/sync/users/hub`, `GET /api/v1/sync/branding/hub`.
- LMS hub branding upsert from tenant applies customer-default slots when `super_admin_customer_id` is present.
- `VerifyCustomerBearerToken` accepts `LMS_SYNC_TOKEN` for LMS subscriptions.
