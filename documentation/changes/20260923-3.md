# Sync debug logs

- `Log::debug` on LMS hub auth, customer-bearer auth, customer list, user hub list, and outbound customer/user pushes. Messages start with `sync:`.
- User upsert and locate accept `origin: lms`.
